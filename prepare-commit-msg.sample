#!/bin/bash
COMMIT_MSG_FILE=$1
git-llm-utils > $COMMIT_MSG_FILE.llm && sed -i "s/^/# /" $COMMIT_MSG_FILE.llm && cat - $COMMIT_MSG_FILE >> $COMMIT_MSG_FILE.llm && mv $COMMIT_MSG_FILE.llm $COMMIT_MSG_FILE
